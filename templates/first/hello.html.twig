<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>sayHello</title>
</head>
<body>
<div>
    <h1>Hello {{ prenom | capitalize}} {{ nom | capitalize}}</h1>
    <h2>Nous sommes le {{'now' | date('d/m/Y')}}</h2>
    <img src="{{ asset('assets/images/') ~ path | defaultImage }}" alt="">
</div>
</body>
</html>